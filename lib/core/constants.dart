import 'package:flutter/material.dart';

const String baseUrl = "http://localhost:8080"; //https://telefoni-backend.onrender.com";

BoxShadow menuItemShadow = const BoxShadow(
    offset: Offset(0, 20),
    blurRadius: 50,
    spreadRadius: 0,
    color: Color(0x1A374557));
